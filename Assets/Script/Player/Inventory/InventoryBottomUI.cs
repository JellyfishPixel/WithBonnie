using UnityEngine;

public class InventoryBottomUI : MonoBehaviour
{
    public InventoryBottomSlotUI[] slots;

    //public void Init(InventoryUIController controller)
    //{
    //    for (int i = 0; i < slots.Length; i++)
    //    {
    //        slots[i].Setup(i, controller);
    //    }
    //}

    //public void Refresh()
    //{
    //    var inv = BoxInventory.Instance;
    //    for (int i = 0; i < slots.Length; i++)
    //    {
    //        slots[i].Refresh(inv.GetSlot(i));
    //    }
    //}
}
